export interface mealUploadType {
    name: string,
    imageUrl: string
}

export interface allMealType {
    name: string,
    imageUrl: string,
    id: string
}

export interface noticeType {
    title: string,
    description: string,
    imageUrl: string
}